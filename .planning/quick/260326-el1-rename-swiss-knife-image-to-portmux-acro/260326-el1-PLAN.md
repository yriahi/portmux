---
phase: quick
plan: 260326-el1
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - CLAUDE.md
  - Dockerfile
  - go.mod
  - README.md
  - docker-compose.yml
  - .github/workflows/build-push.yml
  - test.sh
  - swiss-knife-image.png
  - .claude/skills/add-port/SKILL.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "All source files reference 'portmux' instead of 'swiss-knife-image'"
    - "The project title reads 'portmux' instead of 'Swiss Knife Image'"
    - "The Go binary is named 'portmux' in Dockerfile, test.sh, and .gitignore"
    - "The Go module is named 'portmux' in go.mod"
    - "The Docker registry paths use portmux instead of swiss-knife-image"
    - "The image file is renamed from swiss-knife-image.png to portmux.png"
    - "go vet passes and test.sh passes after rename"
  artifacts:
    - path: "go.mod"
      contains: "module portmux"
    - path: "Dockerfile"
      contains: "/portmux"
    - path: "test.sh"
      contains: "portmux"
    - path: "README.md"
      contains: "# portmux"
  key_links:
    - from: "Dockerfile"
      to: "go.mod"
      via: "module name must match binary name in go build"
      pattern: "go build.*-o /portmux"
    - from: "test.sh"
      to: "go.mod"
      via: "go build output binary name"
      pattern: "go build -o portmux"
---

<objective>
Rename "swiss-knife-image" / "Swiss Knife Image" to "portmux" across all source files.

Purpose: The repo has been renamed to portmux; all internal references must match.
Output: All source files consistently use "portmux" as the project and binary name.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@Dockerfile
@go.mod
@test.sh
@README.md
@docker-compose.yml
@.github/workflows/build-push.yml
@.gitignore
@.claude/skills/add-port/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename binary and module references in build/test files</name>
  <files>.gitignore, go.mod, Dockerfile, test.sh</files>
  <action>
    Perform these exact replacements:

    1. **go.mod** line 1: `module swiss-knife-image` -> `module portmux`

    2. **Dockerfile** three locations:
       - Line 19: `go build -trimpath -o /swiss-knife-image .` -> `go build -trimpath -o /portmux .`
       - Line 28: `COPY --from=build /swiss-knife-image /swiss-knife-image` -> `COPY --from=build /portmux /portmux`
       - Line 32: `ENTRYPOINT ["/swiss-knife-image"]` -> `ENTRYPOINT ["/portmux"]`

    3. **test.sh** four locations:
       - Line 4: `# Swiss Knife Image` -> `# portmux`
       - Line 43: `rm -f swiss-knife-image` -> `rm -f portmux`
       - Line 50: `go build -o swiss-knife-image .` -> `go build -o portmux .`
       - Line 57: `./swiss-knife-image > /tmp/sai-test-stdout.log 2>&1 &` -> `./portmux > /tmp/sai-test-stdout.log 2>&1 &`

    4. **.gitignore** line 2: `swiss-knife-image` -> `portmux`

    After edits, run `go vet ./...` to confirm the module rename is valid.
  </action>
  <verify>
    <automated>cd /Users/yriahi/Development/portmux && go vet ./... && grep -c "swiss-knife-image" go.mod Dockerfile test.sh .gitignore 2>/dev/null | grep -v ":0$" | wc -l | tr -d ' '</automated>
  </verify>
  <done>go.mod says "module portmux", Dockerfile builds/copies/entrypoints "/portmux", test.sh builds/runs/cleans "portmux", .gitignore ignores "portmux". Zero occurrences of "swiss-knife-image" in these 4 files. go vet passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rename project name and registry paths in docs, compose, CI, skill, and image file</name>
  <files>CLAUDE.md, README.md, docker-compose.yml, .github/workflows/build-push.yml, .claude/skills/add-port/SKILL.md, swiss-knife-image.png</files>
  <action>
    1. **Rename the image file:**
       `git mv swiss-knife-image.png portmux.png`

    2. **README.md** — perform these replacements throughout the file:
       - `# Swiss Knife Image` -> `# portmux`
       - `![Swiss Knife Image](swiss-knife-image.png)` -> `![portmux](portmux.png)`
       - All occurrences of `swiss-knife-image` in registry paths -> `portmux`
         (e.g., `nexus.cainc.com:5001/cainc/ops/yriahi/swiss-knife-image` -> `nexus.cainc.com:5001/cainc/ops/yriahi/portmux`)

    3. **CLAUDE.md** — replace within the `GSD:project-start` block:
       - `**Swiss Knife Image**` -> `**portmux**`

    4. **docker-compose.yml** line 3:
       - `image: nexus.cainc.com:5001/cainc/ops/yriahi/swiss-knife-image:latest` -> `image: nexus.cainc.com:5001/cainc/ops/yriahi/portmux:latest`

    5. **.github/workflows/build-push.yml** line 24:
       - `images: nexus.cainc.com:5001/cainc/ops/yriahi/swiss-knife-image` -> `images: nexus.cainc.com:5001/cainc/ops/yriahi/portmux`

    6. **.claude/skills/add-port/SKILL.md** — replace all occurrences:
       - `swiss-knife-image` -> `portmux` (appears in description frontmatter line 4, body line 13, commit message line 130)

    Do NOT touch any files under `.planning/` or `.claude/settings*`.
  </action>
  <verify>
    <automated>cd /Users/yriahi/Development/portmux && grep -c "swiss-knife-image\|Swiss Knife Image" CLAUDE.md README.md docker-compose.yml .github/workflows/build-push.yml .claude/skills/add-port/SKILL.md 2>/dev/null | grep -v ":0$" | wc -l | tr -d ' ' && test -f portmux.png && echo "png_exists"</automated>
  </verify>
  <done>Zero occurrences of "swiss-knife-image" or "Swiss Knife Image" in CLAUDE.md, README.md, docker-compose.yml, build-push.yml, SKILL.md. The image file exists as portmux.png. All registry paths reference portmux.</done>
</task>

<task type="auto">
  <name>Task 3: Run full integration test suite</name>
  <files></files>
  <action>
    Run the full test suite to confirm the renamed binary builds and all tests pass:
    ```bash
    cd /Users/yriahi/Development/portmux && bash test.sh
    ```
    This validates that:
    - `go build -o portmux .` succeeds with the new module name
    - The binary starts and listens on all ports
    - All HTTP response tests pass
    - Graceful shutdown works
    - Cleanup removes `portmux` binary (not the old name)
  </action>
  <verify>
    <automated>cd /Users/yriahi/Development/portmux && bash test.sh</automated>
  </verify>
  <done>All tests pass (0 failures). Binary builds as "portmux" and all HTTP endpoints respond correctly.</done>
</task>

</tasks>

<verification>
- `grep -r "swiss-knife-image\|Swiss Knife Image" .gitignore CLAUDE.md Dockerfile go.mod README.md docker-compose.yml .github/workflows/build-push.yml test.sh .claude/skills/add-port/SKILL.md` returns zero matches
- `test -f portmux.png` confirms image file renamed
- `test ! -f swiss-knife-image.png` confirms old image file gone
- `bash test.sh` passes with 0 failures
</verification>

<success_criteria>
- Zero occurrences of "swiss-knife-image" or "Swiss Knife Image" in any source file (excluding .planning/ and .claude/settings*)
- The Go binary is named "portmux" everywhere (Dockerfile, test.sh, .gitignore, go.mod)
- All registry paths use "portmux" instead of "swiss-knife-image"
- The project title is "portmux" in CLAUDE.md and README.md
- The PNG file is renamed to portmux.png
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/quick/260326-el1-rename-swiss-knife-image-to-portmux-acro/260326-el1-SUMMARY.md`
</output>
