---
phase: quick
plan: 260325-ru5
type: execute
wave: 1
depends_on: []
files_modified:
  - test.sh
  - docker-compose.yml
  - README.md
  - Dockerfile
  - .github/workflows/build-push.yml
  - go.mod
  - CLAUDE.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/RETROSPECTIVE.md
  - .planning/research/STACK.md
  - .planning/research/ARCHITECTURE.md
  - .planning/research/PITFALLS.md
  - .planning/research/FEATURES.md
  - .planning/research/SUMMARY.md
  - .planning/quick/260325-qnw-add-building-and-pushing-section-to-read/260325-qnw-PLAN.md
  - .planning/quick/260325-rbw-add-ports-8000-8888-3306-5432-6379-9090-/260325-rbw-PLAN.md
  - .planning/quick/260325-rbw-add-ports-8000-8888-3306-5432-6379-9090-/260325-rbw-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "All active project files use 'swiss-knife-image' (kebab) and 'Swiss Knife Image' (title)"
    - "Go module builds and tests pass with new module name"
    - "Docker build references correct binary name"
  artifacts:
    - path: "go.mod"
      contains: "module swiss-knife-image"
    - path: "Dockerfile"
      contains: "/swiss-knife-image"
    - path: "test.sh"
      contains: "swiss-knife-image"
    - path: "README.md"
      contains: "Swiss Knife Image"
  key_links:
    - from: "go.mod"
      to: "main.go"
      via: "module name"
      pattern: "module swiss-knife-image"
    - from: "Dockerfile"
      to: "go.mod"
      via: "binary name matches module"
      pattern: "swiss-knife-image"
---

<objective>
Rename the project from "Swiss Army Image" / "swiss-army-image" to "Swiss Knife Image" / "swiss-knife-image" across all active project files.

Purpose: The repo directory has already been renamed to swiss-knife-image; internal references must match.
Output: All files consistently use the new name; build and tests still pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename in source/build/CI files (go.mod, Dockerfile, test.sh, docker-compose.yml, workflows, README.md)</name>
  <files>go.mod, Dockerfile, test.sh, docker-compose.yml, .github/workflows/build-push.yml, README.md</files>
  <action>
    Perform exact string replacements in these files. No regex needed — literal find-and-replace:

    **Kebab-case replacements** (binary names, image tags, module name):
    - `swiss-army-image` -> `swiss-knife-image` (case-sensitive)

    **Title-case replacements** (headings, comments, prose):
    - `Swiss Army Image` -> `Swiss Knife Image` (case-sensitive)

    Files and what to expect:
    1. `go.mod` — line 1: `module swiss-army-image` -> `module swiss-knife-image`
    2. `Dockerfile` — 3 occurrences: build output path, COPY source, ENTRYPOINT binary name
    3. `test.sh` — comment on line 4 ("Swiss Army Image"), `go build -o swiss-army-image`, `./swiss-army-image`, `rm -f swiss-army-image`
    4. `docker-compose.yml` — image tag reference
    5. `.github/workflows/build-push.yml` — images field in metadata step
    6. `README.md` — heading line 1 and all image tag references (approx 5 occurrences of kebab, 1 title)

    IMPORTANT: Do NOT touch files under `.planning/milestones/` or `.claude/worktrees/` — those are archived.

    After replacements, run `go vet ./...` to confirm the module rename does not break compilation.
  </action>
  <verify>
    <automated>cd /Users/yriahi/Development/swiss-knife-image && go vet ./... && grep -c "swiss-army-image" go.mod Dockerfile test.sh docker-compose.yml .github/workflows/build-push.yml README.md 2>/dev/null | grep -v ":0$" | wc -l | tr -d ' '</automated>
  </verify>
  <done>
    - `go vet` passes with new module name
    - Zero occurrences of "swiss-army-image" remain in go.mod, Dockerfile, test.sh, docker-compose.yml, build-push.yml, README.md
    - All replaced with "swiss-knife-image" or "Swiss Knife Image" as appropriate
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename in CLAUDE.md and .planning/ documentation files</name>
  <files>CLAUDE.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/RETROSPECTIVE.md, .planning/research/STACK.md, .planning/research/ARCHITECTURE.md, .planning/research/PITFALLS.md, .planning/research/FEATURES.md, .planning/research/SUMMARY.md, .planning/quick/260325-qnw-add-building-and-pushing-section-to-read/260325-qnw-PLAN.md, .planning/quick/260325-rbw-add-ports-8000-8888-3306-5432-6379-9090-/260325-rbw-PLAN.md, .planning/quick/260325-rbw-add-ports-8000-8888-3306-5432-6379-9090-/260325-rbw-SUMMARY.md</files>
  <action>
    Same literal find-and-replace in all planning/documentation files:

    - `swiss-army-image` -> `swiss-knife-image` (kebab-case)
    - `Swiss Army Image` -> `Swiss Knife Image` (title-case)

    These are documentation files so no compilation check needed — just string replacement.

    IMPORTANT: Do NOT touch files under `.planning/milestones/` or `.claude/worktrees/`.
  </action>
  <verify>
    <automated>cd /Users/yriahi/Development/swiss-knife-image && grep -rn "swiss-army-image\|Swiss Army Image" CLAUDE.md .planning/PROJECT.md .planning/ROADMAP.md .planning/STATE.md .planning/RETROSPECTIVE.md .planning/research/ .planning/quick/260325-qnw*/260325-qnw-PLAN.md .planning/quick/260325-rbw*/260325-rbw-PLAN.md .planning/quick/260325-rbw*/260325-rbw-SUMMARY.md 2>/dev/null | wc -l | tr -d ' '</automated>
  </verify>
  <done>
    - Zero occurrences of "swiss-army-image" or "Swiss Army Image" in any of the listed documentation files
    - All replaced with "swiss-knife-image" or "Swiss Knife Image"
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite to confirm nothing broke</name>
  <files></files>
  <action>
    Run `bash test.sh` from the project root to confirm the renamed binary builds, starts, and all integration tests pass. This validates that the go.mod module rename, Dockerfile binary name, and test.sh binary references are all consistent.
  </action>
  <verify>
    <automated>cd /Users/yriahi/Development/swiss-knife-image && bash test.sh</automated>
  </verify>
  <done>
    - All tests pass (exit 0)
    - Binary builds as "swiss-knife-image"
    - Server starts and responds on all ports
  </done>
</task>

</tasks>

<verification>
1. `go vet ./...` passes — module rename is valid
2. `bash test.sh` passes — binary builds and all HTTP tests succeed
3. `grep -rn "swiss-army-image\|Swiss Army Image" --exclude-dir=.planning/milestones --exclude-dir=.claude` returns zero matches in active files
</verification>

<success_criteria>
- Zero occurrences of "swiss-army-image" or "Swiss Army Image" in any active project file (excluding archived milestones and worktrees)
- Full test suite passes with the new name
- Go module, Dockerfile, CI workflow, docker-compose, README all reference "swiss-knife-image"
</success_criteria>

<output>
After completion, create `.planning/quick/260325-ru5-rename-swiss-army-image-to-swiss-knife-i/260325-ru5-SUMMARY.md`
</output>
