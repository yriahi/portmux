---
phase: quick
plan: 260326-de4
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - README.md
  - .github/workflows/build-push.yml
autonomous: true
requirements: []
must_haves:
  truths:
    - "All active project files reference nexus.cainc.com:5001/cainc/ops/yriahi/swiss-knife-image instead of the old nexus.cainc.com:5000/cainc/yriahi/swiss-knife-image"
    - "No occurrences of the old registry path remain in active project files"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Updated image reference"
      contains: "nexus.cainc.com:5001/cainc/ops/yriahi/swiss-knife-image:latest"
    - path: "README.md"
      provides: "Updated registry references in docs"
      contains: "nexus.cainc.com:5001"
    - path: ".github/workflows/build-push.yml"
      provides: "Updated CI/CD image and registry references"
      contains: "nexus.cainc.com:5001"
  key_links: []
---

<objective>
Update the Nexus registry image reference across all active project files from the old path
`nexus.cainc.com:5000/cainc/yriahi/swiss-knife-image` to the new path
`nexus.cainc.com:5001/cainc/ops/yriahi/swiss-knife-image`.

Purpose: The registry port changed from 5000 to 5001 and the image path now includes an `ops/` segment.
Output: Three files updated with consistent new registry reference.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docker-compose.yml
@README.md
@.github/workflows/build-push.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update registry reference in all active project files</name>
  <files>docker-compose.yml, README.md, .github/workflows/build-push.yml</files>
  <action>
In all three files, perform a find-and-replace of the old registry reference with the new one. Two distinct changes apply:

1. **Registry host:port** — Change `nexus.cainc.com:5000` to `nexus.cainc.com:5001` (appears in login/registry config and all image paths)
2. **Image path** — Change `cainc/yriahi/swiss-knife-image` to `cainc/ops/yriahi/swiss-knife-image` (the `ops/` segment is inserted)

Combined, every occurrence of `nexus.cainc.com:5000/cainc/yriahi/swiss-knife-image` becomes `nexus.cainc.com:5001/cainc/ops/yriahi/swiss-knife-image`.

Specific locations per file:

**docker-compose.yml** (1 occurrence):
- Line 3: `image:` value

**README.md** (5 occurrences):
- `docker login nexus.cainc.com:5000` line — update port to 5001
- `docker buildx build ... -t nexus.cainc.com:5000/cainc/yriahi/...` line — update full path
- `docker run` example referencing the image — update full path
- Compose example `image:` line — update full path
- Ports/info table `Registry` row — update full path (no `:latest` tag on this one)

**.github/workflows/build-push.yml** (2 occurrences):
- `images:` in metadata-action — update full image path
- `registry:` in login-action — update port from 5000 to 5001

Do NOT modify any files under `.planning/` — those are historical records.
  </action>
  <verify>
    <automated>grep -r "nexus.cainc.com:5000" docker-compose.yml README.md .github/workflows/build-push.yml | wc -l | tr -d ' ' | grep -q "^0$" && grep -c "nexus.cainc.com:5001" docker-compose.yml README.md .github/workflows/build-push.yml | grep -v ":0$" | wc -l | tr -d ' ' | grep -q "^3$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Zero occurrences of nexus.cainc.com:5000 remain in active project files. All three files contain nexus.cainc.com:5001/cainc/ops/yriahi/swiss-knife-image references.</done>
</task>

</tasks>

<verification>
- `grep -r "nexus.cainc.com:5000" docker-compose.yml README.md .github/workflows/build-push.yml` returns no results
- `grep -r "nexus.cainc.com:5001/cainc/ops/yriahi/swiss-knife-image" docker-compose.yml README.md .github/workflows/build-push.yml` returns matches in all three files
- `grep -r "cainc/yriahi/swiss-knife-image" docker-compose.yml README.md .github/workflows/build-push.yml | grep -v "ops/"` returns no results (no old path without ops/ segment)
</verification>

<success_criteria>
All active project files (docker-compose.yml, README.md, .github/workflows/build-push.yml) reference nexus.cainc.com:5001/cainc/ops/yriahi/swiss-knife-image consistently. No old references remain.
</success_criteria>

<output>
After completion, create `.planning/quick/260326-de4-update-docker-compose-yml-image-referenc/260326-de4-SUMMARY.md`
</output>
