---
phase: 02-container-and-distribution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - README.md
autonomous: true
requirements:
  - DIST-03
  - CONT-03

must_haves:
  truths:
    - "docker-compose.yml defines the stub service with all 6 ports mapped"
    - "README explains what the image does, shows docker run with all 6 ports, shows docker-compose snippet, and shows sample JSON response"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Single-service compose example with all 6 port mappings"
      contains: "nexus.cainc.com:5000/cainc/yriahi/swiss-army-image"
    - path: "README.md"
      provides: "Usage documentation with docker run command and sample response"
      contains: "docker run"
  key_links:
    - from: "docker-compose.yml"
      to: "nexus.cainc.com:5000/cainc/yriahi/swiss-army-image:latest"
      via: "image reference"
      pattern: "image:.*swiss-army-image"
    - from: "README.md"
      to: "docker-compose.yml"
      via: "references compose file usage"
      pattern: "docker.compose"
---

<objective>
Create the docker-compose.yml usage example and README.md documentation for the Swiss Army Image.

Purpose: Provide users with ready-to-use examples and clear documentation so they can start using the image immediately with a single `docker run` or `docker compose up`.
Output: docker-compose.yml, README.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-container-and-distribution/02-CONTEXT.md

# Phase 1 summary — needed for JSON response shape
@.planning/phases/01-core-server-binary/01-01-SUMMARY.md

# Source files — needed for understanding what the binary does
@handler.go

<interfaces>
<!-- JSON response shape from Phase 1 handler.go -->
```go
type Response struct {
    Port        int               `json:"port"`
    Method      string            `json:"method"`
    Path        string            `json:"path"`
    Timestamp   string            `json:"timestamp"`
    QueryParams map[string]string `json:"query_params"`
}
```

Sample JSON output:
```json
{
  "port": 8080,
  "method": "GET",
  "path": "/",
  "timestamp": "2026-03-25T12:00:00Z",
  "query_params": {}
}
```

Ports served: 80, 8080, 8181, 8081, 3000, 5000

Image reference (per D-02): nexus.cainc.com:5000/cainc/yriahi/swiss-army-image
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <read_first>
    - .planning/phases/02-container-and-distribution/02-CONTEXT.md (D-07 confirmed shape for compose file)
    - handler.go (verify the 6 ports: 80, 8080, 8181, 8081, 3000, 5000)
  </read_first>
  <action>
Create `docker-compose.yml` at the repo root per D-07 (single-service topology).

Exact content:
```yaml
services:
  stub:
    image: nexus.cainc.com:5000/cainc/yriahi/swiss-army-image:latest
    ports:
      - "80:80"
      - "8080:8080"
      - "8181:8181"
      - "8081:8081"
      - "3000:3000"
      - "5000:5000"
```

Key points:
- Service name `stub` (per D-07)
- Image reference uses full Nexus path per D-02 with `:latest` tag per CONTEXT.md specifics section
- All 6 ports mapped host:container
- No `version:` key — deprecated in modern compose; `services:` at top level is correct
- No additional services — D-07 explicitly says "just the stub with all 6 ports mapped; no assumption about what is being scaffolded"
  </action>
  <verify>
    <automated>grep -q 'nexus.cainc.com:5000/cainc/yriahi/swiss-army-image:latest' docker-compose.yml && grep -c '5000' docker-compose.yml | grep -q '[1-9]' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `docker-compose.yml` in repo root
    - Contains `services:` at the top level (no `version:` key)
    - Contains service named `stub`
    - Contains `image: nexus.cainc.com:5000/cainc/yriahi/swiss-army-image:latest`
    - Contains exactly 6 port mappings: `"80:80"`, `"8080:8080"`, `"8181:8181"`, `"8081:8081"`, `"3000:3000"`, `"5000:5000"`
    - Contains only one service (no other services defined)
  </acceptance_criteria>
  <done>docker-compose.yml at repo root with single stub service mapping all 6 ports to the Nexus-hosted image</done>
</task>

<task type="auto">
  <name>Task 2: Create README.md with usage documentation</name>
  <files>README.md</files>
  <read_first>
    - handler.go (verify Response struct fields and JSON tags for sample response body)
    - docker-compose.yml (just created in Task 1 — reference for compose snippet)
    - .planning/phases/02-container-and-distribution/02-CONTEXT.md (D-08 scope: what the image does, docker run, docker-compose snippet, sample JSON response)
  </read_first>
  <action>
Create `README.md` at the repo root per D-08 with these sections:

**1. Title and description:**
- `# Swiss Army Image`
- One paragraph explaining what it does: a lightweight Docker image that acts as a universal HTTP stub for testing containerized service scaffolding, listens on ports 80, 8080, 8181, 8081, 3000, 5000 simultaneously, returns HTTP 200 with JSON request metadata on every path regardless of method

**2. Quick Start — docker run:**
```bash
docker run \
  -p 80:80 \
  -p 8080:8080 \
  -p 8181:8181 \
  -p 8081:8081 \
  -p 3000:3000 \
  -p 5000:5000 \
  nexus.cainc.com:5000/cainc/yriahi/swiss-army-image:latest
```

**3. Quick Start — docker compose:**
Reference the docker-compose.yml file and show the compose snippet:
```yaml
services:
  stub:
    image: nexus.cainc.com:5000/cainc/yriahi/swiss-army-image:latest
    ports:
      - "80:80"
      - "8080:8080"
      - "8181:8181"
      - "8081:8081"
      - "3000:3000"
      - "5000:5000"
```
Then: `docker compose up`

**4. Sample Response:**
Show the JSON response body with all 5 fields:
```json
{
  "port": 8080,
  "method": "GET",
  "path": "/",
  "timestamp": "2026-03-25T12:00:00Z",
  "query_params": {}
}
```
Brief description of each field: `port` (int, the port that received the request), `method` (string, HTTP method), `path` (string, request path), `timestamp` (string, ISO 8601 UTC), `query_params` (object, key-value pairs from query string)

**5. Ports:**
Table listing all 6 ports and their typical framework association:
| Port | Common Framework |
|------|-----------------|
| 80 | nginx, Apache, generic HTTP |
| 8080 | Spring Boot, Tomcat, generic app server |
| 8181 | Karaf, some microservice frameworks |
| 8081 | Alternative app server port |
| 3000 | Node.js (Express, Next.js, React dev) |
| 5000 | Flask, Python dev servers |

Note: Port 80 requires root or CAP_NET_BIND_SERVICE; if it fails to bind, the other 5 ports still work.

**6. Image Details:**
- Base: `FROM scratch` (~5 MB, zero OS overhead)
- Architectures: `linux/amd64`, `linux/arm64`
- Registry: `nexus.cainc.com:5000/cainc/yriahi/swiss-army-image`
- Tags: `:latest`, `:main`, semver (e.g., `:v1.0.0`)

Do NOT include: build instructions (no `go build`), contributing guide, license, badges, or anything outside D-08 scope.
  </action>
  <verify>
    <automated>grep -q 'docker run' README.md && grep -q 'docker compose' README.md && grep -q '"port": 8080' README.md && grep -q 'nexus.cainc.com:5000' README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `README.md` in repo root
    - Contains `# Swiss Army Image` as the title
    - Contains a `docker run` command with all 6 `-p` port flags and the full Nexus image path
    - Contains a docker-compose.yml snippet with `nexus.cainc.com:5000/cainc/yriahi/swiss-army-image:latest`
    - Contains `docker compose up`
    - Contains a sample JSON response with all 5 fields: `"port"`, `"method"`, `"path"`, `"timestamp"`, `"query_params"`
    - Contains a ports table listing all 6 ports (80, 8080, 8181, 8081, 3000, 5000)
    - Contains `FROM scratch` mention in image details
    - Contains `linux/amd64` and `linux/arm64` mention
    - Does NOT contain `go build` or build-from-source instructions
  </acceptance_criteria>
  <done>README.md at repo root with: project description, docker run command with all 6 ports, docker-compose snippet, sample JSON response, port table, and image details</done>
</task>

</tasks>

<verification>
- `docker-compose.yml` has exactly 6 port mappings and references the correct Nexus image
- `README.md` contains all 4 required sections per D-08: what it does, docker run, docker-compose, sample JSON
- Both files reference the correct image path: `nexus.cainc.com:5000/cainc/yriahi/swiss-army-image`
</verification>

<success_criteria>
- docker-compose.yml defines a single stub service with all 6 ports mapped to the Nexus image
- README.md provides complete usage documentation: description, docker run, docker compose, sample response, port reference
- All content uses the correct registry path per D-02
</success_criteria>

<output>
After completion, create `.planning/phases/02-container-and-distribution/02-02-SUMMARY.md`
</output>
